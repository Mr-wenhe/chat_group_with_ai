# S5–S8 详细实施提示词

每次复制一个完整代码块。先完成前置阶段再继续，不把这些未来指令当成本次文档任务的执行授权。

## S5：群聊 @提醒与任务面板的操作闭环

```text
只实施计划 S5。读取 S1–S4 记录和需求 R04/R09/R11；根 AGENTS.md 与
/Users/fengye/.codex/skills/ponytail/SKILL.md 必须遵循，Ponytail full，不自动提交。

目标：需要用户操作时，角色在群聊 @用户，并提供能打开对应任务操作的入口；复用已有面板和动作。
完整阅读 WorkTaskEventStore、WorkTaskPanel、WorkTaskOverlayHost、coordinator 的 approve/reject/
installMissingTool/requestFolderForTask/resume/stop、WorkTaskClarification，
群聊 Message/气泡/@检测/用户人物卡、群成员管理路由及关联 provider。

实现要求：
1. 为补角色、角色不匹配、必要信息、安装、目录授权和命令审批生成持久阻碍，使用 taskId+blockerId+版本。
   相同阻碍状态只发一次角色 @提醒，状态变化必要时更新；没有可用AI才由系统说明，不能伪装角色回复。
2. 群聊“处理/查看任务”入口定位准确任务并显示现有面板。补角色入口可复用成员管理界面，回来重新验证；
   必要问题允许聊天和面板回答。安装/授权/拒绝/取消/稍后继续都必须有实际操作能力。
3. 成功后继续同一任务原检查点；拒绝不是同意降级，稍后保持等待，取消明确停止。
   用户在群内正在查看仍显示消息和按钮，系统通知遵循现有 presence 规则。
4. 同一动作两入口并发点击应幂等；处理中禁重复提交。完成、取消、需求版本变化后旧按钮失效；
   不用“当前最新任务”替代 taskId，不可批准旧范围来执行新请求。
5. 复用事件和消息关联的现有机制；如确需新关联字段，用最小兼容方案并处理 Hive/备份默认值。
   不新建审批平台，不加入纯装饰状态，不让页面拥有任务生命周期。

测试：扩展 panel/chat integration/coordinator 的 widget 和策略测试，验证群聊 @与面板一致、
从旧消息打开准确任务、目录授权成功/选错目录/取消、安装成功/失败、补成员继续、问题回答、
双入口同时批准、需求变更后旧按钮、跨页面和重启去重；验证按钮触发实际回调而非只看文本。
检查可访问性标签、键盘可达、订阅dispose、列表增量更新；不做全量UI重构。
运行相关测试及 flutter analyze --no-pub，按 AGENTS 全维度 review，一次性列出问题。
更新 A06/A10/A14/A15/A18 的覆盖及实施记录，停在 S5。
```

## S6：真实 Word、指定路径和交付门禁

```text
只实施计划 S6。读取本目录完整需求，重点 B03、R08/R09，及 S1–S5 实施记录。
读取根 AGENTS.md、/Users/fengye/.codex/skills/ponytail/SKILL.md，严格 Ponytail full；不自动提交。

目标：明确要求 Word 和桌面时，完成只能对应本次生成、正确位置、真实可读的 DOCX；不能降级 MD。
完整阅读 document_skill_templates、ExpertSkillCatalog、CharacterSkillResolver、WorkModePolicy，
DefaultWorkTaskRunner 的技能注入/command/完成/附件发布、WorkArtifactDeliveryGuard、WorkDocumentTool、
WorkCommandPolicy/Runner、路径授权/文件写入服务及现有 ZIP/文档解析依赖。检索旧 AgentRuntime
二进制降级全部调用方和测试，区分活跃与兼容路径，不凭名字大删代码。

实现要求：
1. 用户显式格式/保存位置优先于通用文档 Skill、已保存旧 Skill 和模型建议。禁止 Word 自动降级指令；
   MD 可作转换源但不计最终交付。不要覆盖/删除用户自定义 Skill 来粗暴消除旧提示词。
2. 优先复用现有 command.run、pandoc 可信安装建议和等待恢复，不新建转换插件市场或 DOCX SDK。
   验证命令调用所需权限；仅授予用户批准的具体操作，不绕过路径/命令/安装审批。
   skill.download 只是技能元数据，安装成功不代表转换程序可用。
3. 转换能力不存在时持久等待+双入口提醒，批准后继续原任务。拒绝/安装失败/权限不足不能标完成、改格式或位置。
4. 完成门禁接入 S1 合同：文件本次生成或修改证据、规范化位置、可读性、格式真实、正文非空及核心内容项。
   DOCX 验证内容类型、Word document XML 和正文，不能只看扩展名/ZIP头；用已有依赖并限制体积/解压资源。
   旧文件/错误目录/伪扩展名/只有Markdown中间文件都不能通过。门禁必须作用于真正的 finish 与消息发布。
5. 文件已保存但附件发送失败时准确区分结果，复用可重试交付，不重复转换或覆写文件。
   处理覆盖原文件的权限和可撤销性，不将转换的副作用藏在只读工具中。
6. 对仍支持的旧路径同步收紧未经同意的降级行为及冲突测试；当前生产路径优先，禁止顺手重构旧运行时全部代码。
   共享交付修复保持私聊兼容，不把群讨论强加给私聊。

验证 A14–A17/A21：真实最小DOCX临时文件、伪DOCX、无正文、陈旧产物、错误目录、转换非零退出、
缺工具安装恢复、目录拒绝再授权、保存成功附件失败、旧模板要求降级、正文含不可信“自动授权”指令。
测试用临时目录和模拟工具，不使用真实桌面或 Key；最终真机Word可打开验证留到 S8。
运行相关测试/静态分析，检查隐私、路径规范化、文件损失、进程注入与大文件资源上限。
更新实施记录及旧测试变更理由，输出所有未通过项；停在 S6。
```

## S7：恢复、兼容、安全与生命周期收口

```text
只实施计划 S7。读取 S1–S6 记录、需求 R10–R13、根 AGENTS.md 和
/Users/fengye/.codex/skills/ponytail/SKILL.md，严格 Ponytail full，不自动提交。

目标：新讨论与交付流程跨页面、跨重启及压缩后仍正确，旧数据/普通聊天/私聊/审批边界不回归。
阅读本轮全部 diff、WorkTaskCoordinator 生命周期和资源锁、work_mode_task_recovery、
WorkAgentLoop checkpoint 白名单、WorkContextBuilder、任务事件存储、备份任务相关处理、
前台 watcher/自动聊天隔离及所有新增订阅。只修本次范围，不做全仓清理。

实现要求：
1. 覆盖讨论中、等用户、等角色、待安装、待授权、执行中、附件失败各状态的离开和恢复。
   保存理解依据/百分比、最新请求版本、角色、取舍、合同、FIFO和原路径；不得从100%恢复成任务已完成。
2. 压缩保留关键约束及未决问题；事件/任务 JSON 安全解析未知字段和旧版本，不能因重启丢阻碍或扩权。
   旧完成任务不伪造讨论；旧未完成任务按新规则补讨论，保留已完成操作及有效审批，变更范围重新审批。
3. 资源锁与会话控制器维持单执行人；等待阶段不忙轮询，不占用活跃模型调用，无后台/离线自动生成。
   取消后晚到回调不更新任务、不发完成消息、不继续安装/命令；停止行为遵循已有可取消边界。
4. 群聊与任务面板动作幂等，重复事件/重启不重复 @用户、不复用旧审批操作新目标。
   群、私聊隔离，普通聊天与自动聊天无工具入口。附件/成员/技能文本无法改变执行人、合同和授权。
5. 检查Hive字段、生成文件、备份恢复对新JSON/消息关联的兼容；只有必要时按规则生成代码。
   对恢复状态中不可移植路径仍遵循既有备份策略，不扩大授权。

测试 A03/A18/A19/A21 及并发恢复：可控时钟与future模拟页面销毁、断点恢复、摘要压缩、
未知旧版本、并发补充、两个任务等待同一授权、重复安装结果、撤销授权、错误会话ID、恶意附件指令。
运行所有 test/work_mode 及受影响 chat/agentic 测试、flutter analyze --no-pub。
检查是否触及平台插件/依赖：若无变更不要新增依赖；有则验证 Flutter 3.24 fork 的 Android 兼容红线。
全面 review 并集中列全问题，不能一个测试失败就停止其他独立检查。
更新实施记录、剩余验收清单和真实环境限制；不要宣告整体验收，停在 S7。
```

## S8：完整验收、真实产物与规格同步

```text
只执行计划 S8 的全链路验收和必要修复。读取 S1–S7 记录及全部代码 diff，
根 AGENTS.md、/Users/fengye/.codex/skills/ponytail/SKILL.md 和完整需求必须遵循。
Ponytail full，最小根因修复；不自动 commit/push/release，不在无授权时安装真实软件或写用户桌面。

先明确验收范围为本次全部改动及 A01–A21，建立逐项检查表，不只复测前三个偏差。
实现/补齐从真实输入路由→角色讨论→理解进度→用户操作→执行→交付的集成测试，
沿真实 provider/coordinator 路径运行，避免只拼接几个孤立 helper 后宣称端到端。

核心场景：
1. 截图原句输入，@all召集、最终产品经理、Word、桌面：多职业发言、产品经理主动取舍进度，
   不自动开发HTML，100%后该角色输出真实DOCX；测试断言身份、顺序、文件格式及路径。
2. 无指定HTML任务公开推举前端；没有前端或指定错人时@用户，可补角色继续。
3. 必要问题必须等待；讨论补充即时合并；执行补充FIFO修改原文件；旧任务完成后的新任务重新选人。
4. 缺转换工具及路径授权同时有任务/群聊入口，成功恢复、拒绝保留、取消停止、稍后跨重启可继续。
5. 短/中/复杂讨论、成员错误、无进展收敛、伪100%、旧回复竞态、损坏DOCX和附件失败均有结果。

运行 flutter analyze --no-pub 和 flutter test --no-pub；若依赖未就绪说明原因，按授权范围解决，
不要静默更新依赖版本。构建检查按实际改动范围执行，必要时验证平台构建，遵守签名失败关闭规则。
已有失败和新增失败分开记录，但都继续完成不依赖失败项的检查。

真实UI验收：先确认当前可用测试环境/模型/临时可写目录，隔离用户数据；模拟网关通过不能替代真实模型质量。
有明确测试权限时运行真实多角色讨论、原生审批入口与真实Word文件打开检查，并保留脱敏证据。
桌面写入和工具安装通过APP审批完成；若缺环境或需用户完成动作，明确标记该项未检查，不能编造通过。
至少检查Word正文、标题、核心章节、保存位置、执行人身份、聊天讨论和按钮后续操作。

逐项review需求符合性、正确性、边界和失败路径、测试有效性、可读性、架构、安全、性能、新老兼容，
并覆盖注释/命名/常量/函数规模/封装/文件规模/解耦/模块化/监听释放。发现问题不立即结束验收，
收集全部问题按严重性列文件行号、证据、影响和修复方向。完成必要最小修复后重跑相关受影响检查。

更新 docs/work_mode_agent_v1_requirements.md、相关技术设计和新验收报告，清楚说明
“群聊先讨论后单执行人”取代哪些旧接力/默认降级条款，私聊边界是什么；保留历史报告事实，不能回写历史通过结论。
最终报告列出 A01–A21 的通过/失败/未检查、所有命令、真实环境证据、剩余风险。
只有规格对照、逐行审查、自动测试和必要真实验收全部完成且无阻塞，才可宣布新需求验收通过。
若尚有未检查项，明确结论为未完成验收并给出具体后续操作；不要以“测试全绿”代替验收。
```
