# S1–S4 详细实施提示词

使用方式：每次复制一个完整代码块。以下是供后续编码使用的指令，本次文档任务不执行这些指令。仓库根目录为 `/Volumes/new_disk/work/flutter/chat_group/chat_group`。

## S1：请求语义、最终执行人与产物合同

```text
请只实施 docs/group_work_discussion/03-implementation-plan.md 的 S1。
先读取根 AGENTS.md、/Users/fengye/.codex/skills/ponytail/SKILL.md、
docs/group_work_discussion/{01-findings,02-requirements,03-implementation-plan}.md。
严格执行 Ponytail full：读真实调用链，先复用，再做最小根因修复；不能删减已确认需求。
检查 git status，保留现有改动。本阶段获准修改代码和必要测试，不自动 commit/push。

目标：解决 B02 的请求语义根因，并为 B03 建立产物合同；本阶段不造讨论调度器。
先完整阅读 chat_room_agentic_input_support、chat_room_utils、WorkRoleRouter 及 planner/models、
WorkRoleModelSelectorService、AICharacter、CharacterSkillResolver、WorkHandoffState。
检索这些入口及提及解析器全部调用方，区分当前生产入口和旧 selectExecutor。

实现要求：
1. 分离 @all 讨论召集、@某人咨询和明确最终委托。@all 不拒绝群任务，也不能覆盖“最后由 @X 输出”。
   普通聊天提及语义保持兼容。多义/未知/重名返回可用于等待澄清的结构，不随机换人。
2. 从最终交付意图生成最小产物合同：类型、格式、位置、内容范围、显式执行人、修订目标及请求版本。
   “讨论 HTML 游戏后由产品经理出 Word”只要求 Word PRD，不自动变成开发/测试实际接力。
3. 职业资格来自配置的职业、能力、职责。名字/标签/全局 Skill/临时自荐不能单独证明资格。
   HTML 开发需要前端能力；产品经理可整理 Word 需求文档，转换能力单独检查。
4. 未指定时返回合格候选及可展示的依据，供后续群内推举使用；不要把第一个候选直接等同最终推举完成。
   不合格指定人不得被自动替换；模型路由返回必须本地核验，不以一个 confidence 数字绕过资格。
5. 优先扩展现有策略/类型；避免职业注册框架、独立 NLP 服务或全新依赖。

验证：扩展 work_role_router_test、相关 mention/skill 测试，覆盖 @all+最终委托、咨询+最终委托、
Word PRD 含 HTML、纯 HTML、全栈明确前端能力、测试角色误指定、无候选、未知/重名、名字伪能力、
角色英文职责、明确新任务不继承旧指定人、普通聊天及私聊边界。
运行受影响测试和 flutter analyze --no-pub；失败后继续检查其他独立项。
不要写仅断言源码含字符串的测试。输出实际改动、复用决定、命令结果、A04–A06/A20 的覆盖与未完成部分。
按 AGENTS 十项质量及安全、兼容、失败路径完整自检，将阶段记录写入本目录的实施记录。
不要宣告整个功能完成，不自动进入 S2。
```

## S2：持久讨论状态与执行前门禁

```text
请只实施 docs/group_work_discussion/03-implementation-plan.md 的 S2，读取 S1 实施记录及实际 diff。
读取根 AGENTS.md、/Users/fengye/.codex/skills/ponytail/SKILL.md 和本目录需求、定位、计划。
严格 Ponytail full；保留现有工作区改动，不自动提交。

目标：在现有唯一 WorkTaskCoordinator 下持久化群讨论和等待状态，使未讨论/未理解的任务不能调用执行工具。
完整阅读 AgentTask、WorkTaskCoordinator 的 submit/schedule/resume/stop/restore、
DefaultWorkTaskRunner 入口、WorkAgentLoop/checkpoint、WorkContextBuilder、WorkTaskEvent、
WorkTaskClarification、资源锁与 work_task_providers，检索所有 executionStateJson 编解码及白名单。

实现要求：
1. 优先用现有 executionStateJson 的有版本扩展和一个最小编解码入口保存：阶段、请求版本、协调人/执行人、
   参与记录、轮次、理解依据/百分比、未决问题、合同及阻碍。不要新建数据库/第二协调器/通用工作流框架。
2. 群任务可以在暂未选定执行人时持久等待。AgentTask 当前 characterId 必填，处理其承载方式时确保
   “临时协调人”不会被误当最终工具执行人；没有可用角色时保持系统待处理记录，不造虚构 AI。
3. 唯一实际执行入口检查：最新请求版本、讨论达标、关键阻碍为空、理解 100%、角色资格与可用性有效。
   模型 tool/finish、自述已理解或旧检查点不能绕过；审批权限仍独立检查。
4. 讨论保留会话串行归属，不占用写文件资源锁长期等用户；暂停/取消释放恰当资源，晚到回复不能复活任务。
5. 新字段接入 checkpoint 白名单、摘要压缩和恢复。旧记录缺字段安全处理，不声称旧任务已讨论；
   已完成产物保留；旧未完成任务按需求补足讨论，不能回放旧已完成命令。
6. 尽量不改 HiveType/枚举；若确需模型字段变更遵守旧数据默认值及生成器规则，不手改 g.dart。

验证：扩展 coordinator/checkpoint/context/recovery 测试，覆盖合法准备→执行、99%拒绝、
100%但关键问题未决、请求版本过期、无执行人、暂停重启、取消后晚返回、跨群数据、旧记录和压缩后合同保持。
使用可控 runner 断言“工具未被调用”，不能仅断言状态文案。
运行相关测试及 flutter analyze --no-pub；按 AGENTS 全维度 review 并一次性报告全部问题。
记录 A11/A19 的覆盖、兼容决策和 S3 接入点，更新实施记录；不要提前开放未完整实现的用户流程，停在 S2。
```

## S3：真实多角色讨论、主动汇总和理解进度

```text
只实施本目录计划 S3。先读取 S1/S2 实施记录、当前实现、完整需求 R02–R07 和 A01/A02/A07–A09。
读取根 AGENTS.md 与 /Users/fengye/.codex/skills/ponytail/SKILL.md，严格 Ponytail full，
优先复用现有网关、消息存储、事件流及状态，不建立第二任务引擎，不自动提交。

目标：让成员真正以自身职业/模型配置在群聊讨论，由协调人/执行人积极组织，理解到 100% 后自动执行。
阅读 AiRequestGateway 及现有流式调用、角色上下文构建、消息存储、WorkPublicUpdateStream、
普通自动聊天抑制逻辑、S2 协调状态，查所有调用方与取消机制。

实现要求：
1. 首轮给各可参与成员机会，后续按未决问题定向发言。每名角色调用自己的 API 配置；不让一个模型扮演全群，
   不套普通随机 1–2 人/自动聊天循环。停用、无凭据、超时有真实记录，关键意见缺失不得假装已解决。
2. 有执行人由其主持收集与取舍；没有则群中推举临时协调人并公开推举合格最终执行人，选择必须经 S1 校验。
   无合格职业转等待补角色，不自行改派。决定者应在每轮汇总，也能在若干成员回复后主动追问。
3. 实现复杂度参考 1–2/2–4/4–6 轮和连续两轮无实质进展的收敛；允许复杂任务说明理由延长，
   不硬凑轮数、不一人一句秒结束、不无限跑。建议收集间隔和超时常量集中，等待模型状态不冒充 AI 发言。
4. 理解程度独立于执行进度，以目标/范围/方案/建议取舍/格式位置/验收明确程度为依据。
   每次协调人或执行人讨论回复显示百分比、简明缺项；执行人接手重评。未决关键项禁止 100%。
   只存公开结论/理由/建议，不能展示私有推理。成员建议不是用户授权。
5. 讨论提示词分工明确：成员只贡献其职责判断；执行人负责追问、取舍、缺用户信息时请求澄清；
   结构化返回由本地校验，不能只靠 prompt 保证 100% 门禁。禁止副作用工具进入讨论回合。
6. 达标后通过 S2 唯一入口直接执行，无额外方案确认；上下文使用最新合同和公开共识。

测试：用可控多角色网关验证实际 characterId 调用顺序/角色身份、全员机会、成员二次发言、
执行人不长期静默、复杂度差异、无进展退出、缺必要信息等待、模型虚报100%、模型超时/异常/取消、
工具调用在讨论完成前为零；不把固定文案当语义测试。必要 widget 测试核验多成员气泡和百分比。
运行相关测试、静态分析；检查摘要大小、调用预算、监听释放与无忙轮询。
更新阶段记录和全部发现，标明真实模型质量仍需 S8 验收；不要继续 S4。
```

## S4：讨论补充合并、执行 FIFO 与新任务边界

```text
只实施计划 S4。读取此前阶段记录、需求 R01/R07/R10/R12、现有输入路由与 coordinator.enqueueFollowUp。
读取根 AGENTS.md、/Users/fengye/.codex/skills/ponytail/SKILL.md，严格 Ponytail full；不自动提交。

目标：讨论期间直接合并补充，执行期间保留 FIFO；已完成后的新任务重新讨论和选人，不继承错误旧执行人。
完整阅读 chat_room_agentic_input_support 的 latest task 查询与 _runWorkModeTask、
WorkFollowUpPolicy、coordinator 的 promote/clarification/revision 分支、附件队列和资源释放逻辑。
先列明实际调用链，保留“恢复任务占用控制器、不取消旧任务、不丢新输入、同文件修订覆盖原路径”的红线。

实现要求：
1. 按当前阶段分派。讨论补充写入当前请求版本及附件上下文，下一讨论/汇总使用新版本；
   已在途回复可结束，但过期 100%/finish 不能触发执行。涉及目标变化应重评复杂度和理解进度。
2. 执行阶段新输入持久 FIFO，群聊确认已收到及稍后处理，不取消当前执行。执行完成后针对修改做适量增量讨论，
   保留相同 task/原产物关联和明确的原文件路径；不能按重名另建文件躲避修订。
3. 回答必要信息/补角色的输入唤醒对应阻碍，不混作新的独立任务。待审批时的补充若改变操作范围，
   旧审批需失效后按新范围申请，不能扩大旧授权。
4. 完成后的“新做一个…”等明确新任务建立新讨论与选人；“修改刚才/继续完善”沿用原成果。
   不能仅凭存在最新未取消任务就跳过路由；无法判定且会影响执行人/文件时 @用户澄清。
5. 统一状态处理入口，复用现有附件 ID 队列和 follow-up 策略，不在 widget 和 coordinator 各写一套规则。

验证 A10/A12/A13/A20：讨论模型在途时连续补充、补充后旧完成返回、执行中两条中英同文件修订、
带附件FIFO、执行失败后仍保留新输入、澄清回答、完成后换执行人的新任务、跨页面恢复、取消明确不继续。
用延迟可控 future 验证竞态，不依赖真实 sleep；断言真实目标路径和调用次序。
运行相关测试和静态分析，全面 review 边界、数据完整性、版本一致性和老调用方。
更新实施记录：修复前后触发例子、命令结果、剩余风险和下一阶段入口。停在 S4。
```
