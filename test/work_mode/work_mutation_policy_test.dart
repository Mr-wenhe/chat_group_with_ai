import 'work_change_policy_test.dart' as change_policy_tests;

/// Stage 02 design filename; the shared test body lives in the focused change
/// policy test so both public naming variants exercise the same matrix.
void main() => change_policy_tests.main();
