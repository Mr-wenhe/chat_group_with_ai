# Agentic 文件生成 Bug —— 彻底修复报告

> 状态：**代码已修复并通过全部测试，等待你的实测验收（尚未提交）。**

## 诚实结论

上一轮（首次修复）只解决了"表面症状"（文件内容不再塞进聊天气泡、模板水印已移除），
**核心逻辑一个没动**——所以你实测后 5 个抱怨全中。本轮把剩下的核心问题一次性根除。

## 5 个抱怨的根因与修复

| # | 你的抱怨 | 真实根因 | 修复 |
|---|---------|---------|------|
| 1 | 生成的不是我要的内容 | `enableLocalFilePlanner` 默认 `true`，**完全绕过 LLM** 写死空壳模板 `<p>内容由AI生成</p>` | 生产侧关闭本地规划器，改由 LLM 生成真实内容（`args.content` 放完整内容） |
| 2 | 贴了 2 个文件 | `allowRenameOnConflict` 在文件已存在时自动改名 `page_2.html`；第一层防御过激阻断多步流程 | 移除自动改名（已存在→清晰报错）；移除过激第一层防御，仅保留"已写文件则拒再次写"的第二层 |
| 3 | 代码没写入还贴出来 | 畸形 `<tool_call\n<function=...>` 变体未被解析也未清洗 | 强化 sanitize（清洗 `<function=`/`<parameter=` 孤立片段）+ `tryParse` 兼容 `<parameter=NAME>` 等号语法 |
| 4 | 追问时 AI 失忆 | `complete()` 只传 system prompt，从不传对话历史 | `AgentRuntime` 增加 `conversationHistory` 参数；`ChatRoomPage._agenticHistory()` 传最近 12 条消息 |
| 5 | 审批是文字输入不是弹层 | DM 自动批准要求消息含"文件"二字（说"生成个人主页"被拒→弹文字审批） | 私聊 `autoApproveWriteTools: isDirect`（无条件自动批准）；需审批时用 `AlertDialog` 弹层代替打字 |

## 修改文件

- `lib/features/agentic/agent_runtime.dart` — 关 planner（生产侧）、移除自动改名、移除过激第一层防御、conversationHistory 透传、强化 sanitize
- `lib/features/agentic/tool_request.dart` — 兼容 `<parameter=NAME>` 等号语法
- `lib/features/agentic/agent_prompt_builder.dart` — planning prompt 改为"完整内容放 args.content"
- `lib/features/chat_group/chat_room_page.dart` — `_agentRuntimeFor` 关 planner、DM 自动批准、`_agenticHistory()`、`_showAgentApprovalDialog()`、审批改弹层
- `test/agentic/agent_runtime_permission_test.dart` — rename 测试改为"拒绝覆盖"

## 验证

- `flutter analyze`：0 error（仅 scripts/test 预存 info/warning）
- `flutter test` 全量：**All tests passed!** EXIT_CODE=0（agentic suite 25/25）

## 请你按以下步骤实测（验收通过后我再提交）

1. `flutter run -d macos`，进入范晓萌**私聊**
2. 发：「帮我生成一个个人主页的 html 文件」
   - ✅ 不应再弹审批（私聊自动批准）；若弹出也必须是**弹层按钮**而非打字
   - ✅ 回复只有 1 句话 + 文件卡片，**不塞大段代码**
   - ✅ 打开 `page.html`：是真实生成的网页内容，**不是流星雨、不是空壳 `<p>`**
3. 再发：「写一个 shell 脚本」
   - ✅ `.sh` 文件里**没有** "Generated by:" 之类的签名
4. 追问：「刚才那个主页加个导航栏」
   - ✅ AI 应记得刚才生成了主页（不再失忆），并生成更新版
5. 观察回复风格：无论成功失败都不应在聊天里贴代码
